---
description: "Aprova a execução de uma operação já registrada, sem executar nada"
argument-hint: "[operação ou deixe vazio para escolher]"
---

Execute `skills/performance-ads-roteador/SKILL.md` com intenção fixa `aprovacao`. Se a operação não foi informada, liste as abertas do cliente com `scripts/dossier.py list --open` e confirme pelo título antes de aprovar. Mostre no chat o que será executado (em blocos, sem hashes) e registre com `dossier.py approve`. Não execute.
